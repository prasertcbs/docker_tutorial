create database foody;

use foody;

create table food(id int, descr varchar(10));

insert into food values (1, 'egg'), (2, 'rice');

select * from food;

insert into food values (3, 'meat ball'), (4, 'hotdog');

select * from food;